{{- if .Values.client.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "checkmate.fullname" . }}-client
  namespace: {{ include "checkmate.namespace" . }}
  labels:
    {{- include "checkmate.componentLabels" (dict "root" . "component" "client") | nindent 4 }}
  {{- $ann := merge (default dict .Values.client.ingress.annotations) (default dict .Values.global.annotations) }}
  {{- if $ann }}
  annotations:
    {{- toYaml $ann | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.client.ingress.className }}
  {{- if .Values.client.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.client.ingress.host }}
      secretName: {{ include "checkmate.clientIngressTLSSecretName" . }}
  {{- end }}
  rules:
    - host: {{ .Values.client.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "checkmate.fullname" . }}-client
                port:
                  number: {{ .Values.client.port }}
{{- end }}
